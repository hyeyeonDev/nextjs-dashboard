import { Inter, Lusitana, Noto_Sans_KR, Ubuntu } from 'next/font/google';

export const inter = Inter({ subsets: ['latin'] });

export const lusitana = Lusitana({
  weight: ['400', '700'],
  subsets: ['latin'],
});

export const notoSansKr = Noto_Sans_KR({
  weight: ['400', '700'],
  subsets: ['latin'],
});

export const ubuntu = Ubuntu({
  weight: ['400', '700'],
  subsets: ['latin'],
});
